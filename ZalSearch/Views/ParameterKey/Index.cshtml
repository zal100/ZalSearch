@model IEnumerable<ZalSearch.Models.ParameterKeys>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TermValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TermValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParameterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParameterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParameterId })
        </td>
    </tr>
}

</table>
